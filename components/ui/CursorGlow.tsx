'use client';

import { useEffect, useRef } from 'react';

interface CursorGlowProps {
    color?: string;
    size?: number;
}

export default function CursorGlow({ color = 'rgba(45,212,191,0.12)', size = 600 }: CursorGlowProps) {
    return null;
}
